-- Layout de tarjeta (bg_filepath, avatar/text coords, text_color).
-- Las columnas se aseguran también en ensureCoreTables (ADD COLUMN suave).
-- Este archivo marca la versión de schema en Drizzle sin fallar si ya existen.
SELECT 1;
