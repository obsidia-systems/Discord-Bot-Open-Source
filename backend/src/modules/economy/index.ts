import type { AdobosModule } from "../../core/modules/types.js";

/** Stub Lego — listo para comandos/rutas de economía. */
export const economyModule: AdobosModule = {
  id: "economy",
  name: "Economía",
  register(ctx) {
    void ctx;
  },
};
