# Feature: moderation

Stub Lego de **Moderación**. La UI real aún no está; las rutas viven en `pages/dashboard/moderation/`.

Cuando se implemente: builders aquí, thin pages sin lógica, comandos/rutas en `backend/src/modules/moderation/`.
