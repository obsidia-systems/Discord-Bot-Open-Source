---
import PanelLayout from "./PanelLayout.astro";

interface Props {
  title: string;
  description?: string;
  compactContent?: boolean;
}

const { title, description, compactContent } = Astro.props;
---

<PanelLayout title={title} description={description} compactContent={compactContent}>
  <slot />
</PanelLayout>
