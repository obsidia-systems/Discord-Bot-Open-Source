# Discord
DISCORD_TOKEN=
DISCORD_CLIENT_ID=
DISCORD_GUILD_ID=

# Panel / API
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# SQLite
# - Sin Docker (pnpm dev en el host): ruta relativa al workspace
DATABASE_URL=file:./data/database.sqlite
# - Con docker compose (dev/prod): docker-compose.yml fuerza file:/data/database.sqlite
#   montado en ./data del host → no se corrompe entre reinicios
